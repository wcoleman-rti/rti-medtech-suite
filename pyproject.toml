[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
markers = [
    "benchmark: performance benchmark tests (require observability stack)",
]
